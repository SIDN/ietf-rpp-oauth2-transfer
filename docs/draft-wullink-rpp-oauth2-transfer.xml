<?xml version="1.0" encoding="utf-8"?>
<!-- name="GENERATOR" content="github.com/mmarkdown/mmark Mmark Markdown Processor - mmark.miek.nl" -->
<rfc version="3" ipr="trust200902" docName="draft-wullink-rpp-oauth2-transfer-00" submissionType="IETF" category="std" xml:lang="en" xmlns:xi="http://www.w3.org/2001/XInclude" indexInclude="true" tocDepth="4">

<front>
<title abbrev="Secure Object Transfer for RPP">Secure Object Transfer for RESTful Provisioning Protocol (RPP)</title><seriesInfo value="draft-wullink-rpp-oauth2-transfer-00" stream="IETF" status="standard" name="Internet-Draft"></seriesInfo>
<author initials="M." surname="Wullink" fullname="Maarten Wullink"><organization>SIDN Labs</organization><address><postal><street></street>
</postal><email>maarten.wullink@sidn.nl</email>
<uri>https://sidn.nl/</uri>
</address></author><author initials="P." surname="Kowalik" fullname="Pawel Kowalik"><organization>DENIC</organization><address><postal><street></street>
</postal><email>pawel.kowalik@denic.de</email>
<uri>https://denic.de/</uri>
</address></author><date year="2026" month="April" day="30"></date>
<area>Internet</area>
<workgroup>Network Working Group</workgroup>

<abstract>
<t>This document describes how OAuth 2.0 <xref target="RFC6749"></xref> can be used to secure object transfers in RESTful Provisioning Protocol (RPP) <xref target="I-D.wullink-rpp-core"></xref>.</t>
</abstract>

</front>

<middle>

<section anchor="introduction"><name>Introduction</name>
<t>TODO</t>
</section>

<section anchor="terminology"><name>Terminology</name>
<t>In this document the following terminology is used.</t>
<t>REST - Representational State Transfer (<xref target="REST"></xref>). An architectural style.</t>
<t>RESTful - A RESTful web service is a web service or API implemented using HTTP and the principles of <xref target="REST"></xref>.</t>
<t>EPP RFCs - This is a reference to the EPP version 1.0 specifications <xref target="RFC5730"></xref>, <xref target="RFC5731"></xref>, <xref target="RFC5732"></xref> and <xref target="RFC5733"></xref>.</t>
<t>RESTful Provisioning Protocol or RPP - The protocol described in this document.</t>
<t>URL - A Uniform Resource Locator as defined in <xref target="RFC3986"></xref>.</t>
<t>Resource - An object having a type, data, and possible relationship to other resources, identified by a URL.</t>
<t>RPP client - An HTTP user agent performing an RPP request</t>
<t>RPP server - An HTTP server responsible for processing requests and returning results in any supported media type.</t>
<t>JWT - JSON Web Token as defined in <xref target="RFC7519"></xref>.</t>
</section>

<section anchor="conventions-used-in-this-document"><name>Conventions Used in This Document</name>
<t>The key words &quot;MUST&quot;, &quot;MUST NOT&quot;, &quot;REQUIRED&quot;, &quot;SHALL&quot;, &quot;SHALL NOT&quot;,&quot;SHOULD&quot;, &quot;SHOULD NOT&quot;, &quot;RECOMMENDED&quot;, &quot;MAY&quot;, and &quot;OPTIONAL&quot; in this document are to be interpreted as described in <xref target="RFC2119"></xref>.</t>
<t>In examples, indentation and white space in examples are provided only to illustrate element relationships and are not REQUIRED features of the protocol.</t>
<t>All example requests assume a RPP server using HTTP version 2 is listening on the standard HTTPS port on host rpp.example. An authorization token has been provided by an out of band process and MUST be used by the client to authenticate each request.</t>
</section>

<section anchor="object-transfer-authorization"><name>Object Transfer Authorization</name>
<t>OAuth 2.0 Rich Authorization Requests <xref target="RFC9396"></xref> extends the standard OAuth 2.0 authorization request with an <tt>authorization_details</tt> parameter that carries a structured JSON object describing precisely what the client is requesting authorization for. Unlike scopes, which are coarse-grained string tokens, <tt>authorization_details</tt> allows the request to include typed, fine-grained authorization data, such as the specific object being transferred. The  authorization server can present to the user in a meaningful consent screen.</t>
<t>In RPP, RAR is used for interactive federated object transfers to convey the specific domain name or contact handle being transferred. The gaining registrar includes an <tt>authorization_details</tt> object of type <tt>rpp_transfer</tt> in the authorization request to the losing registrar's authorization server. The registrant then sees exactly which object they are consenting to transfer. The authorization server MUST echo the <tt>authorization_details</tt> object back as a claim in the issued JWT, giving the registry verifiable, tamper-proof evidence of what was authorized and for which object.</t>
<t>The <tt>type</tt> field MUST be set to <tt>rpp_transfer</tt>. Table <xref target="tbl-rar"></xref> lists the RAR fields defined for RPP.</t>
<table anchor="tbl-rar"><name>RPP Transfer Authorization, RAR <tt>authorization_details</tt> object (Primary Method, <xref target="RFC9396"></xref>)</name>
<thead>
<tr>
<th>Field</th>
<th>Type</th>
<th>Requirement</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>type</tt></td>
<td>String</td>
<td>REQUIRED</td>
<td>MUST be <tt>rpp_transfer</tt>.</td>
</tr>

<tr>
<td><tt>object_type</tt></td>
<td>String</td>
<td>REQUIRED</td>
<td>The RPP object type being transferred. MUST be one of <tt>domain</tt> or <tt>contact</tt>.</td>
</tr>

<tr>
<td><tt>object_identifier</tt></td>
<td>String</td>
<td>REQUIRED</td>
<td>The fully qualified name or handle of the specific object to be transferred (e.g., <tt>foo.example</tt> or <tt>CID-12345</tt>).</td>
</tr>
</tbody>
</table><t>Example RAR <tt>authorization_details</tt> value for a domain transfer:</t>

<sourcecode type="json"><![CDATA[[{
  "type": "rpp_transfer",
  "object_type": "domain",
  "object_identifier": "foo.example"
}]
]]>
</sourcecode>
<t>The losing registrar's authorization server MUST echo the <tt>authorization_details</tt> back as a claim in the issued JWT. The registry MUST validate the <tt>authorization_details</tt> claim in the token and MUST verify that <tt>object_type</tt> and <tt>object_identifier</tt> match the object being transferred.</t>
<t>When the losing registrar's authorization server does not support RAR (<xref target="RFC9396"></xref>), the specific object being transferred MUST be conveyed via the <tt>rpp_object_id</tt> claim rather than encoded in the scope string:</t>

<ul spacing="compact">
<li>To authorize a domain transfer, the specific domain name MUST be present in the <tt>rpp_object_id</tt> claim (e.g., <tt>foo.example</tt>).</li>
<li>To authorize a contact transfer, the specific contact identifier MUST be present in the <tt>rpp_object_id</tt> claim (e.g., <tt>REG-12345</tt>).</li>
</ul>
</section>

<section anchor="claims"><name>Claims</name>
<t>RPP access tokens MUST conform to the JWT Profile for OAuth 2.0 Access Tokens defined in <xref target="RFC9068"></xref>. This profile establishes a standardized set of claims that RPP uses to make authorization decisions consistently across implementations and identity providers.</t>

<section anchor="rpp-specific-claims"><name>RPP-Specific Claims</name>
<t>In addition to the standard <xref target="RFC9068"></xref> claims, table <xref target="tbl-rpp-claims"></xref> lists the RPP-specific claims that are defined to enable fine-grained authorization decisions. Required claims MUST be present in every RPP access token. Optional claims SHOULD be included when applicable to the deployment or request context.</t>
<table anchor="tbl-rpp-claims"><name>RPP Specific Access Token Claims</name>
<thead>
<tr>
<th>Claim</th>
<th>Requirement</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>

<tbody>
<tr>
<td><tt>rpp_transfer_authinfo</tt></td>
<td>OPTIONAL</td>
<td>String</td>
<td>The transfer authorization information used in Fallback flow object transfer. When present, the value MUST be equivalent to the value that would otherwise be sent in the <tt>RPP-Authorization</tt> header, using the same <tt>&lt;method&gt; &lt;authorization information&gt;</tt> format. The RPP server MUST treat this claim as equivalent to the <tt>RPP-Authorization</tt> header; if both are present in the same request, the claim value MUST take precedence. This claim MUST only be present in access tokens used for transfer operations.</td>
</tr>

<tr>
<td><tt>rpp_object_id</tt></td>
<td>OPTIONAL</td>
<td>String</td>
<td>The identifier of the specific object being transferred. MUST be present in access tokens for interactive federated transfers when the fallback dynamic scope method is used (<tt>transfer:domain</tt> or <tt>transfer:contact</tt>) and the <tt>authorization_details</tt> claim (<xref target="RFC9396"></xref>) is not present. The value MUST be the fully qualified name or handle of the object (e.g., <tt>foo.example</tt> or <tt>CID-12345</tt>). The RPP server MUST validate that this value matches the object being transferred. This claim MUST NOT be present when <tt>authorization_details</tt> is present.</td>
</tr>

<tr>
<td><tt>authorization_details</tt></td>
<td>OPTIONAL</td>
<td>Array of Objects</td>
<td>Rich authorization details as defined by <xref target="RFC9396"></xref>. MUST be present in access tokens for interactive federated transfers when the RAR method is used. Each object in the array MUST have a <tt>type</tt> field; for RPP transfer tokens the <tt>type</tt> MUST be <tt>rpp_transfer</tt> and the object MUST include <tt>object_type</tt> and <tt>object_identifier</tt> fields (see <xref target="tbl-rar"></xref>). The RPP server MUST validate that <tt>object_type</tt> and <tt>object_identifier</tt> match the object being transferred. When this claim is present, the <tt>rpp_object_id</tt> claim MUST NOT be present.</td>
</tr>
</tbody>
</table></section>
</section>

<section anchor="object-transfer"><name>Object Transfer</name>
<t><strong>NOTE:</strong> this is very early draft of how a transfer could work in RPP, it is added here to provide a complete picture of the transfer flow and to be able to discuss the details of the transfer flow and the security mechanisms that can be used to secure the transfer flow. This section is expected to be significantly revised and updated as the transfer flow and the security mechanisms are further developed and refined.</t>
<t>The server MAY support a secure object transfer mechanism based on OAuth 2.0 federation for secure object transfer between registrars, the gaining registrar can obtain an access token from the losing registrar's authorization server to authorize the transfer request. This provides a secure mechanism for transferring objects without exposing sensitive information in the transfer request or response messages. This also prevents the use of opaque transfer tokens and their inherent security risks, such as token leakage or replay attacks.
If any of the parties in the transfer flow does not support the secure transfer mechanism based on OAuth 2.0 federation, then the transfer MUST fall back to the Fallback flow transfer mechanism using opaque transfer tokens, using the <tt>RPP-Authorization</tt> header to include the transfer token in the transfer request messages.</t>
<t>The two transfer mechanisms cover complementary scenarios:</t>

<ul>
<li><t><strong>Interactive flow (OAuth 2.0 federated):</strong> The current registrant has an account at the losing registrar and actively initiates or approves the transfer themselves — for example, a registrant migrating their own domain to a new registrar. The registrant authenticates at the losing registrar's authorization server and grants explicit, object-scoped consent.</t>
</li>
<li><t><strong>Fallback flow (authinfo):</strong> The losing registrar has not registered an authorization server URI with the registry, or the domain has been sold and the new owner has no account at the losing registrar. In this case interactive consent at the losing registrar is not possible. The seller (the previous owner, who does have an account at the losing registrar) obtains a transfer authorization code (authinfo) out-of-band and passes it to the buyer. The buyer provides the authinfo to the gaining registrar, which includes it in the transfer request via the <tt>RPP-Authorization</tt> header or the <tt>rpp_transfer_authinfo</tt> JWT claim. The registry validates the authinfo and executes the transfer without requiring the new owner to have any account at the losing registrar.</t>
</li>
</ul>
<t><strong>Flow selection is not at the discretion of the gaining registrar.</strong> The gaining registrar MUST use whichever flow the losing registrar's capabilities dictate, as determined by registry discovery. Specifically:</t>

<ul spacing="compact">
<li>If the registry's discovery endpoint returns an authorization server URI for the losing registrar, the gaining registrar MUST use the interactive OAuth 2.0 federated flow. Fallback flow MUST NOT be used as a substitute, even if it would be simpler to implement. The only exception is if the new owner is a different party than the previous owner (e.g., the domain was sold), in which case the gaining registrar MUST use Fallback flow because the new owner cannot authenticate to the losing registrar's authorization server to give consent.</li>
<li>If the registry's discovery endpoint returns no authorization server URI for the losing registrar, the gaining registrar MUST use Fallback flow.</li>
</ul>
<t>The registry MUST enforce this: if a Fallback flow transfer request (i.e., a request carrying an <tt>RPP-Authorization</tt> header or <tt>rpp_transfer_authinfo</tt> claim) is received for an object whose losing registrar has a registered authorization server URI, the registry MUST reject the request with an appropriate error response. This prevents downgrade attacks and ensures that registrars cannot bypass the stronger OAuth 2.0 flow for operational convenience once they have declared support for it.</t>
<t><strong>Token single-use enforcement.</strong> Access tokens issued by the losing registrar's authorization server for use in the interactive federated transfer flow MUST be single-use. The registry MUST reject any transfer request that presents a token that has already been used to authorize a transfer, even if the token has not yet expired. The losing registrar's authorization server MUST issue tokens with the intent of single use, and SHOULD include a unique <tt>jti</tt> (JWT ID) claim in each token to facilitate replay detection. The registry MUST maintain a short-lived cache of observed <tt>jti</tt> values and MUST reject any token whose <tt>jti</tt> has already been seen.</t>
<t>If the registry cannot enforce single-use semantics (e.g., due to lack of shared state in a distributed deployment), the token lifetime MUST be set as short as operationally feasible, on the order of a few seconds, to minimize the window of opportunity for replay attacks. Token lifetimes for interactive transfer tokens MUST NOT exceed 60 seconds.</t>
<t>For the secure transfer mechanism based on OAuth 2.0 federation, the RPP server requires an established trust relationship with the authorization server of the losing registrar. The server MUST support the necessary mechanisms for validating tokens issued by the losing registrar's authorization server.
this requires the server to support the JWT profile for OAuth 2.0 Access Tokens <xref target="RFC9068"></xref> and to support the necessary mechanisms for validating tokens issued by external authorization servers. The client MUST include a valid access token in the Authorization header of the transfer request, and the server MUST validate the token and the associated permissions before allowing the transfer to proceed. The server MUST also implement appropriate error handling for cases where the token is invalid, expired, or does not have the necessary permissions for the requested transfer operation. Registrars implementing secure transfer mechanisms based on OAuth 2.0 federation MUST support the federated identity provider function.</t>

<section anchor="federation-trust-model"><name>Federation Trust Model</name>
<t>The RPP federation model uses the registry as the central trust anchor....</t>
<t><strong>Token validation without bilateral trust.</strong> When the registry receives a transfer request carrying a JWT issued by the losing registrar's authorization server, it validates the token locally using the losing registrar's public key that was registered at onboarding. No runtime call to the losing registrar or its authorization server is required. The registry already trusts that public key because it was registered through the accreditation process.</t>
<t><strong>Gaining registrar.</strong> The gaining registrar only needs to trust the registry. It obtains the losing registrar's authorization server URI from the registry's discovery endpoint, redirects the registrant's browser there, and exchanges the resulting authorization code for a token. It does not need to pre-configure or authenticate the losing registrar. The browser redirect and back-channel token exchange are both secured by standard HTTPS and do not require a federated identity relationship between the two registrars.</t>
<t>The following diagram shows how authorization server URIs and public keys flow through the registry, and how the gaining registrar uses them at transfer time to redirect the registrant's browser:</t>
<figure><name>RPP Federation Trust Model — Onboarding, Discovery and Browser Redirect
</name>
<sourcecode type="ascii"><![CDATA[  Gaining              Registry             Losing
  Registrar          (Trust Anchor)        Registrar
     |                    |                    |
     |                    |                    |
     : --- Onboarding --- : --- Onboarding --- :
     |                    |                    |
     | Register own AS    |                    |
     | URI + JWKS pubkey  |                    |
     +------------------->|                    |
     |                    |                    |
     |                    | Register own AS    |
     |                    | URI + JWKS pubkey  |
     |                    |<-------------------|
     |                    |                    |
     : --- Transfer time -------------------- :
     |                    |                    |
     | Discover Losing    |                    |
     | Registrar AS URI   |                    |
     +------------------->|                    |
     |                    |                    |
     | AS URI returned    |                    |
     |<-------------------|                    |
     |                    |                    |
     | Redirect registrant's browser           |
     | to Losing Registrar AS                  |
     +---------------------------------------->|
     |                    |                    |
     | Auth code returned via browser redirect |
     |<----------------------------------------|
     |                    |                    |
     | Exchange auth code |                    |
     | for access token   |                    |
     | (back-channel)     |                    |
     +---------------------------------------->|
     |                    |                    |
     | JWT (signed by     |                    |
     | Losing Reg AS)     |                    |
     |<----------------------------------------|
     |                    |                    |
     | Transfer request   |                    |
     | + Bearer JWT       |                    |
     +------------------->|                    |
     |                    |                    |
     |                    | Validate JWT using |
     |                    | cached Losing Reg  |
     |                    | JWKS pubkey        |
     |                    | (no runtime call)  |
     |                    |------------------> |
     |                    |                    |
     | Transfer result    |                    |
     |<-------------------|                    |
     |                    |                    |
]]>
</sourcecode>
</figure>
<t><strong>Security properties.</strong> This model provides the following guarantees:</t>

<ul spacing="compact">
<li>A rogue registrar cannot forge a transfer token that the registry will accept, because only the legitimate losing registrar's public key (registered at onboarding) can produce a valid signature.</li>
<li>The gaining registrar cannot forge registrant consent, because the token is issued by the losing registrar's authorization server, not the gaining registrar.</li>
<li>The registry controls the set of trusted authorization servers by controlling which registrar IdP metadata it accepts at onboarding.</li>
<li>Registrars need no knowledge of each other beyond what the registry exposes via the discovery endpoint.</li>
</ul>
</section>

<section anchor="interactive-flow"><name>Interactive Flow</name>
<t>The interactive flow uses the OAuth 2.0 Authorization Code grant <xref target="RFC6749" sectionFormat="bare" section="Section 4.1"></xref> to obtain explicit, object-specific registrant consent directly from the losing registrar's authorization server. Before redirecting the registrant, the gaining registrar MUST first query the registry's registrar discovery endpoint to resolve the losing registrar's authorization server's authorization URI. If no authorization server URI is available for the losing registrar, the gaining registrar MUST fall back to the Fallback flow described in <xref target="fallback-flow"></xref>.</t>
<t>The authorization request MUST convey the specific object being transferred so that the losing registrar's authorization server can present the registrant with an accurate consent screen. The <strong>primary method</strong> is Rich Authorization Requests (RAR) <xref target="RFC9396"></xref>: the gaining registrar MUST include an <tt>authorization_details</tt> parameter of type <tt>rpp_transfer</tt> (see <xref target="tbl-rar"></xref>). When the losing registrar's authorization server does not support RAR, the <strong>fallback method</strong> is to request a dynamic transfer scope of the form <tt>&lt;object&gt;:transfer</tt> (e.g., <tt>domain:transfer</tt>) and the object identifier MUST be added to the <tt>rpp_object_id</tt> claim. In either case, the resulting token MUST be bound to the single identified object and MUST NOT be accepted by the registry for any other object.</t>
<t>The following diagram illustrates the interactive OAuth 2.0 federated secure object transfer flow:</t>
<figure><name>OAuth 2.0 Federated Secure Object Transfer - Interactive Flow
</name>
<sourcecode type="ascii"><![CDATA[  Client          Gaining         Registry        Losing
(Registrant)    Registrar                        Registrar
     |               |               |               |
     | 1. Initiate   |               |               |
     |  transfer of  |               |               |
     |  foo.example  |               |               |
     +-------------->|               |               |
     |               |               |               |
     |               | 2. Lookup     |               |
     |               |  losing reg   |               |
     |               |  AS URI      |               |
     |               +-------------->|               |
     |               |               |               |
     |               | 3. Return     |               |
     |               |  losing reg   |               |
     |               |  AS URI      |               |
     |               |<--------------|               |
     |               |               |               |
     | 4. Redirect   |               |               |
     |  to Losing Reg|               |               |
     |  AS (RAR:    |               |               |
     |  authz_details|               |               |
     |  type=rpp_    |               |               |
     |  transfer,    |               |               |
     |  id=example   |               |               |
     |  .com)        |               |               |
     |<--------------|               |               |
     |               |               |               |
     | 5. Auth &     |               |               |
     |  approve      |               |               |
     |  transfer at  |               |               |
     |  Losing Reg   |               |               |
     |  AS           |               |               |
     +---------------------------------------------->|
     |               |               |               |
     | 6. Auth code  |               |               |
     |  + redirect to|               |               |
     |  Gaining Reg  |               |               |
     |  callback URI |               |               |
     |<----------------------------------------------|
     |               |               |               |
     | 7. Follow     |               |               |
     |  redirect     |               |               |
     |  (auth code   |               |               |
     |  delivered)   |               |               |
     +-------------->|               |               |
     |               |               |               |
     |               | 8. Exchange   |               |
     |               |  auth code for|               |
     |               |  access token |               |
     |               +------------------------------>|
     |               |               |               |
     |               | 9. JWT token  |               |
     |               |  (authz_      |               |
     |               |  details=     |               |
     |               |  {type:rpp_   |               |
     |               |  transfer,    |               |
     |               |  id:example   |               |
     |               |  .com})       |               |
     |               |<------------------------------|
     |               |               |               |
     |               | 10. Transfer  |               |
     |               |  request +    |               |
     |               |  Bearer token |               |
     |               +-------------->|               |
     |               |               |               |
     |               |               | 11. Local JWT |
     |               |               |  validation   |
     |               |               |  (losing reg  |
     |               |               |  AS pubkey,  |
     |               |               |  verify scope)|
     |               |               |               |
     |               |               | 12. Execute   |
     |               |               |  transfer &   |
     |               |               |  notify Losing|
     |               |               |  Registrar    |
     |               |               +-------------->|
     |               |               |               |
     |               | 13. Transfer  |               |
     |               |   successful  |               |
     |               |<--------------|               |
     |               |               |               |
     |  14. Transfer |               |               |
     |   confirmed   |               |               |
     |<--------------|               |               |
     |               |               |               |
]]>
</sourcecode>
</figure>
<t>The steps in the diagram are as follows:</t>

<ol spacing="compact">
<li>The client initiates a transfer request to the gaining registrar, specifying the object to transfer (e.g., <tt>foo.example</tt>).</li>
<li>The gaining registrar queries the registry's registrar discovery endpoint to look up the losing registrar's authorization server authorization URI. The losing registrar is identified from the current sponsoring registrar data on the domain.</li>
<li>The registry returns the losing registrar's authorization server authorization URI. If no URI is registered, the gaining registrar MUST fall back to the Fallback flow.</li>
<li>The gaining registrar redirects the client's browser to the losing registrar's authorization server. The authorization request MUST include an <tt>authorization_details</tt> parameter of type <tt>rpp_transfer</tt> with <tt>object_type: &quot;domain&quot;</tt> and <tt>object_identifier: &quot;foo.example&quot;</tt> (RAR, <xref target="RFC9396"></xref>). If the authorization server does not support RAR, the fallback dynamic scope <tt>transfer:domain:foo.example</tt> MUST be used instead. The gaining registrar's callback URI is included as the OAuth 2.0 <tt>redirect_uri</tt>.</li>
<li>The client authenticates at the losing registrar's authorization server and explicitly approves the transfer scope, providing direct, verifiable consent to release the specific object.</li>
<li>The losing registrar's authorization server issues an authorization code and redirects the client's browser back to the gaining registrar's registered callback URI.</li>
<li>The client's browser follows the redirect, delivering the authorization code to the gaining registrar's callback endpoint.</li>
<li>The gaining registrar exchanges the authorization code for an access token at the losing registrar's authorization server token endpoint (back-channel, Authorization Code grant <xref target="RFC6749" sectionFormat="bare" section="Section 4.1"></xref>).</li>
<li>The losing registrar's authorization server validates the code and issues a signed JWT access token (<xref target="RFC9068"></xref>). When RAR was used, the token MUST contain an <tt>authorization_details</tt> claim echoing the <tt>rpp_transfer</tt> object. When the dynamic scope fallback was used, the token MUST contain the <tt>transfer:domain</tt> (or <tt>transfer:contact</tt>) scope in the <tt>scope</tt> claim and MUST include the <tt>rpp_object_id</tt> claim carrying the specific object identifier.</li>
<li>The gaining registrar submits the transfer request to the registry, including the JWT as a Bearer token in the <tt>Authorization</tt> header.</li>
<li>The registry validates the JWT locally using the losing registrar's authorization server public key (obtained via OAuth 2.0 Authorization Server Metadata <xref target="RFC8414"></xref>). No live call to the losing registrar is required. The registry MUST verify the transfer authorization using the appropriate method: if the token contains an <tt>authorization_details</tt> claim (RAR), the registry MUST verify that the <tt>type</tt> is <tt>rpp_transfer</tt> and that <tt>object_identifier</tt> matches the object being transferred; if the token uses the dynamic scope fallback, the registry MUST verify that the <tt>scope</tt> claim contains <tt>transfer:domain</tt> or <tt>transfer:contact</tt> and that the <tt>rpp_object_id</tt> claim value matches the object being transferred.</li>
<li>The registry executes the transfer and notifies the losing registrar.</li>
<li>The registry returns a successful transfer response to the gaining registrar.</li>
<li>The gaining registrar confirms the completed transfer to the client.</li>
</ol>
</section>

<section anchor="fallback-flow"><name>Fallback Flow</name>
<t>The fallback Flow uses opaque transfer authorization tokens to authorize object transfers between registrars. The gaining registrar obtains the transfer authorization token out-of-band from the losing registrar (e.g., via the registrant or a prior registrar-to-registrar agreement) and includes it in the transfer request. No OAuth 2.0 federation or end-user interaction is required. The registry validates the opaque token against the losing registrar's records to authorize the transfer.</t>
<t>The transfer authorization token MUST be conveyed using the <tt>rpp_transfer_authinfo</tt> claim embedded in a JWT access token.</t>
<t>The following diagram illustrates the machine-to-machine Fallback flow for a secure object transfer:</t>
<figure><name>Fallback Object Transfer - Machine to Machine flow
</name>
<sourcecode type="ascii"><![CDATA[  Client          Gaining         Registry        Losing
(Registrant)    Registrar                        Registrar
     |               |               |               |
     |   1. Initiate |               |               |
     |    transfer   |               |               |
     |   (authz token|               |               |
     |    provided   |               |               |
     |    out-of-band|               |               |
     |    by losing  |               |               |
     |    registrar) |               |               |
     +-------------->|               |               |
     |               |               |               |
     |               | 2. Transfer   |               |
     |               |  request      |               |
     |               |  (RPP-Authori-|               |
     |               |   zation:     |               |
     |               |   <token>)    |               |
     |               +-------------->|               |
     |               |               |               |
     |               |               | 3. Validate   |
     |               |               |  authz token  |
     |               |               |               |
     |               |               | 4. Execute    |
     |               |               |  transfer &   |
     |               |               |  notify Losing|
     |               |               |  Registrar    |
     |               |               +-------------->|
     |               |               |               |
     |               | 5. Transfer   |               |
     |               |   successful  |               |
     |               |<--------------|               |
     |               |               |               |
     |  6. Transfer  |               |               |
     |   confirmed   |               |               |
     |<--------------|               |               |
     |               |               |               |
]]>
</sourcecode>
</figure>
<t>The steps in the diagram are as follows:</t>

<ol spacing="compact">
<li>The client initiates a transfer request to the gaining registrar, providing the transfer authorization token that was obtained out-of-band from the losing registrar (e.g., via the registrant or a prior registrar-to-registrar agreement).</li>
<li>The gaining registrar submits the transfer request to the registry, including the opaque authorization token in the <tt>RPP-Authorization</tt> header.</li>
<li>The registry validates the authorization token against the losing registrar's records.</li>
<li>The registry executes the transfer and notifies the losing registrar.</li>
<li>The registry returns a successful transfer response to the gaining registrar.</li>
<li>The gaining registrar confirms the completed transfer to the client.</li>
</ol>
</section>
</section>

<section anchor="iana-considerations"><name>IANA Considerations</name>
<t>TODO</t>
</section>

<section anchor="internationalization-considerations"><name>Internationalization Considerations</name>
<t>TODO</t>
</section>

<section anchor="security-considerations"><name>Security Considerations</name>
<t>TODO</t>
</section>

<section anchor="change-history"><name>Change History</name>

<section anchor="version-00"><name>Version 00</name>

<ul spacing="compact">
<li>Created initial draft with core concepts and flows.</li>
</ul>
</section>
</section>

</middle>

<back>
<references><name>Normative References</name>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml3/reference.I-D.wullink-rpp-core.xml"/>
<reference anchor="REST" target="http://www.ics.uci.edu/~fielding/pubs/dissertation/rest_arch_style.htm">
  <front>
    <title>Architectural Styles and the Design of Network-based Software Architectures</title>
    <author fullname="Roy Fielding" initials="R." surname="Fielding">
      <organization></organization>
    </author>
    <date year="2000"></date>
  </front>
</reference>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.2119.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.3986.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.5730.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.5731.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.5732.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.5733.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.6749.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.7519.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.8414.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9068.xml"/>
<xi:include href="https://bib.ietf.org/public/rfc/bibxml/reference.RFC.9396.xml"/>
</references>

<section anchor="acknowledgements" numbered="false"><name>Acknowledgements</name>
<t>TODO</t>
</section>

</back>

</rfc>
